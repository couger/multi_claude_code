# 待实现功能

本文档记录已规划但尚未实现的功能，供后续开发参考。

---

## 高优先级

### 1. 全局键盘快捷键
- **快捷键列表**：
  - `Ctrl+N` — 新建会话
  - `Ctrl+W` — 关闭当前会话
  - `Ctrl+Tab` / `Ctrl+Shift+Tab` — 切换会话
  - `Ctrl+1~9` — 快速跳转到第 N 个会话
  - `Escape` — 关闭面板/展开视图
- **实现方式**：渲染进程 `keydown` 事件监听
- **工作量**：约 2 小时

### 2. 系统托盘收纳
- **功能**：
  - 最小化到托盘（而非任务栏）
  - 托盘图标 + 右键菜单
  - 托盘菜单：新建会话、显示窗口、退出
  - 告警时闪烁提醒
- **实现方式**：Electron `Tray` API
- **工作量**：约 2-3 小时

---

## 中优先级

### 3. Web 访问响应式适配
- **目标**：浏览器访问时适配电脑/平板/手机
- **改动**：
  - 侧边栏：移动端改为底部抽屉或汉堡菜单
  - 终端：xterm 触摸滚动和缩放优化
  - 按钮尺寸：增大触摸目标
  - 展开视图：移动端全屏
- **工作量**：约 4-6 小时

### 4. 会话"自动批准"功能
- **背景**：Claude Code CLI 执行某些操作时需要用户确认
- **实现方式**（需先调研 CLI）：
  - 方式A：如果 CLI 支持 `--auto-approve` 参数，直接添加
  - 方式B：监听输出，自动发送确认指令
- **工作量**：方式A 约 1-2 小时，方式B 约 4-6 小时
- **前提**：先确认 Claude CLI 是否支持自动批准参数

### 5. 终端输出搜索
- **功能**：在展开视图中搜索输出内容
- **特性**：
  - 支持正则表达式
  - 高亮匹配结果
  - 上下导航按钮
- **工作量**：约 2-3 小时

---

## 低优先级

### 6. 系统通知
- **功能**：
  - 任务完成时发送桌面通知
  - 需要用户输入时提醒
  - 可配置通知开关
- **工作量**：约 1-2 小时

### 7. 会话模板
- **功能**：
  - 保存常用配置（工作目录、环境变量、启动命令）
  - 一键从模板创建会话
  - 模板管理界面
- **工作量**：约 2-3 小时

### 8. 会话历史
- **功能**：
  - 记录已关闭会话的输出
  - 历史列表查看
  - 可重新打开（基于历史配置）
- **工作量**：约 3-4 小时

### 9. 主题设置
- **功能**：
  - 终端颜色自定义
  - 预设主题选择
  - 字体大小调整
- **工作量**：约 2-3 小时

### 10. 会话导出
- **功能**：
  - 导出终端输出为文本文件
  - 支持带时间戳格式
  - 批量导出
- **工作量**：约 1-2 小时

---

## 实现顺序建议

1. 全局键盘快捷键（最常用，提升体验明显）
2. 系统托盘收纳（桌面应用标配功能）
3. Web 访问响应式适配（扩展使用场景）
4. 会话自动批准（需先调研 CLI）
5. 其他功能按需实现

---

*最后更新：2026-04-11*