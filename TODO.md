# 待实现功能

本文档记录已规划但尚未实现的功能，供后续开发参考。

---

## 高优先级

### 1. Electron 窗口消失 Bug 修复
- **问题描述**：Electron 窗口有概率会消失，除了任务管理器中能看到进程外，无法从任何其他方式找回窗口
- **可能原因**：
  - 窗口位置/大小状态异常（屏幕外、尺寸为零）
  - 窗口 `hide()` 后未正确 `show()`
  - 多显示器切换后窗口坐标失效
- **修复方向**：
  - 启动时检测窗口是否在可见屏幕范围内，若不在则重置位置
  - 增加窗口状态的兜底恢复逻辑
  - 配合托盘图标提供"显示窗口"入口
- **工作量**：约 2-3 小时

### 2. 系统托盘模式
- **功能**：
  - 最小化到系统托盘（而非任务栏）
  - 托盘图标 + 左键/右键菜单
  - 左键点击：显示/隐藏主窗口
  - 右键菜单：新建会话、显示窗口、所有会话状态概览、退出
  - 告警时托盘图标闪烁提醒
- **实现方式**：Electron `Tray` + `Menu` API
- **工作量**：约 3-4 小时

### 3. ESC 键操作确认
- **问题描述**：误点击 ESC 键会停止当前 Claude Code 工作，需要增加确认机制
- **实现方式**：
  - 拦截 ESC 键事件，弹出确认对话框
  - 确认选项："继续工作" / "停止当前任务"
  - 可选：设置中允许用户配置 ESC 行为（直接退出 / 需确认 / 忽略）
- **工作量**：约 1-2 小时

### 4. 会话区域复制粘贴
- **问题描述**：当前会话区域（xterm 终端）无法复制和粘贴，只能手动输入
- **实现方向**：
  - 确保 xterm.js 的 clipboard 集成正常工作
  - 添加显式的复制/粘贴按钮到工具栏
  - 支持标准快捷键（Ctrl+C 复制选中内容，Ctrl+V 粘贴）
  - 注意与终端信号（Ctrl+C 中断进程）的冲突处理
- **工作量**：约 2-3 小时

### 5. 告警气泡优化
- **问题描述**：当前告警气泡面积太大，需要优化
- **优化方向**：
  - 缩小单个告警气泡的默认展示尺寸
  - 优化文本截断和内容布局
  - 鼠标悬停时展示完整内容
- **工作量**：约 1-2 小时

### 6. 鼠标一键消除警告
- **功能**：点击某按钮即可一键清除所有当前显示的告警
- **实现方式**：在告警面板添加"全部清除"按钮
- **工作量**：约 0.5 小时

### 7. 鼠标一键静默警告
- **功能**：点击后静默所有告警，仅在告警图标上显示未读数字
- **实现方式**：
  - 添加"静默模式"切换按钮
  - 静默时隐藏气泡，仅显示数字角标
  - 可随时切换回正常模式查看详情
- **工作量**：约 1-2 小时

### 8. 设置页 - 告警及日志页
- **功能**：在设置页的"网络"页和"AI助手"页之间，新增"告警及日志"页签
- **告警分级功能**：
  - 对常见 Claude Code 异常进行分级（严重/警告/信息/调试）
  - 用户可选择关注哪些级别的告警、过滤哪些
- **告警过滤规则**：
  - 展示常见 Claude Code 异常提醒列表
  - 用户可勾选启用/禁用特定告警类型
  - 支持关键词过滤
- **提醒方式配置**：
  - 每种告警可独立配置提醒方式：不提醒 / 弱提醒（气泡） / 强提醒（弹窗+声音）
  - 全局提醒方式开关
- **工作量**：约 4-6 小时

---

## 中优先级

### 9. 全局键盘快捷键
- **快捷键列表**：
  - `Ctrl+N` — 新建会话
  - `Ctrl+W` — 关闭当前会话
  - `Ctrl+Tab` / `Ctrl+Shift+Tab` — 切换会话
  - `Ctrl+1~9` — 快速跳转到第 N 个会话
- **实现方式**：渲染进程 `keydown` 事件监听
- **工作量**：约 2 小时

### 10. Web 访问响应式适配
- **目标**：浏览器访问时适配电脑/平板/手机
- **改动**：
  - 侧边栏：移动端改为底部抽屉或汉堡菜单
  - 终端：xterm 触摸滚动和缩放优化
  - 按钮尺寸：增大触摸目标
  - 展开视图：移动端全屏
- **工作量**：约 4-6 小时

### 11. 会话"自动批准"功能
- **背景**：Claude Code CLI 执行某些操作时需要用户确认
- **当前状态**：已有 `--dangerously-skip-permissions` 参数透传，缺少应用级自动确认
- **实现方式**：
  - 方式A：如果 CLI 支持 `--auto-approve` 参数，直接添加
  - 方式B：监听输出，自动发送确认指令
- **工作量**：方式A 约 1-2 小时，方式B 约 4-6 小时

### 12. 终端输出搜索
- **功能**：在展开视图中搜索输出内容
- **特性**：
  - 支持正则表达式
  - 高亮匹配结果
  - 上下导航按钮
- **工作量**：约 2-3 小时

---

## 低优先级

### 13. 系统通知
- **功能**：
  - 任务完成时发送桌面通知（OS 级别）
  - 需要用户输入时提醒
  - 可配置通知开关
- **工作量**：约 1-2 小时

### 14. 会话模板
- **功能**：
  - 保存常用配置（工作目录、环境变量、启动命令）
  - 一键从模板创建会话
  - 模板管理界面
- **工作量**：约 2-3 小时

### 15. 会话历史
- **当前状态**：已有持久化存储，缺少 UI 展示
- **功能**：
  - 记录已关闭会话的输出
  - 历史列表查看界面
  - 可重新打开（基于历史配置）
- **工作量**：约 3-4 小时

### 16. 主题设置
- **功能**：
  - 终端颜色自定义
  - 预设主题选择
  - 字体大小调整
- **工作量**：约 2-3 小时

### 17. 会话导出
- **当前状态**：后端 IPC 已实现，缺少 UI 入口
- **功能**：
  - 在活跃组件中添加"导出日志"按钮
  - 支持带时间戳格式
  - 批量导出
- **工作量**：约 1-2 小时

---

## 实现顺序建议

1. Electron 窗口消失 Bug 修复（影响基本可用性）
2. 系统托盘模式（配合窗口恢复 + 桌面应用标配）
3. ESC 键操作确认（防误操作，体验提升明显）
4. 会话区域复制粘贴（基本交互需求）
5. 告警气泡优化 + 一键消除 + 一键静默（体验优化，关联紧密）
6. 设置页 - 告警及日志页（告警体系完善）
7. 全局键盘快捷键（提升操作效率）
8. 其他功能按需实现

---

*最后更新：2026-04-17*
